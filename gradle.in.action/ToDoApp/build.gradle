ext.projectIds = [
  'group': 'com.afecioru.todo',
  'version': '0.1'
]

allprojects {
  group = projectIds.group
  version = projectIds.version
}

subprojects {
  apply plugin: 'java-library'
  apply plugin: 'java'

  repositories {
    mavenCentral()
  }
}
