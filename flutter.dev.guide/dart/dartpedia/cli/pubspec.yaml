name: cli
description: A sample command-line application.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  http: ^1.3.0
  logging: ^1.2.0
  command_runner: ^1.0.0
  wikipedia: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
